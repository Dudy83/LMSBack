'use strict';

/**
 * categories-activite service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::categories-activite.categories-activite');
