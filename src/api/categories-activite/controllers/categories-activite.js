'use strict';

/**
 *  categories-activite controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::categories-activite.categories-activite');
