'use strict';

/**
 * categories-activite router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::categories-activite.categories-activite');
