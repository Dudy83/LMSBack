'use strict';

/**
 * formations-categorie service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formations-categorie.formations-categorie');
